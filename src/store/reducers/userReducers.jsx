// src/store/reducers/userReducer.js

const initialState = {
    // начальное состояние пользователя
  };
  
  const userReducer = (state = initialState, action) => {
    switch (action.type) {
      // обработка действий для пользователя
      default:
        return state;
    }
  };
  
  export default userReducer;
  