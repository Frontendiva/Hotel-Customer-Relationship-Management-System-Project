// src/store/reducers/roomReducer.js

const initialState = {
    // начальное состояние комнаты
  };
  
  const roomReducer = (state = initialState, action) => {
    switch (action.type) {
      // обработка действий для комнаты
      default:
        return state;
    }
  };
  
  export default roomReducer;
  